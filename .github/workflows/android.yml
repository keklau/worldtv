name: Build Android APK (WorldTV – stable Gradle 8.7 + AGP 8.5.2)

on:
  workflow_dispatch: {}
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Force Gradle 8.7 (évite Gradle 9.0)
      - name: Set up Gradle 8.7
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 8.7
          cache-read-only: true

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Generate Android project (AGP 8.5.2, SDK 34, Media3)
        run: |
          set -e
          mkdir -p app/src/main/java/com/worldtv
          mkdir -p app/src/main/res/layout
          mkdir -p app/src/main/res/values

          # settings.gradle (repositories modernes)
          cat > settings.gradle <<'EOF'
          pluginManagement {
            repositories { gradlePluginPortal(); google(); mavenCentral() }
          }
          dependencyResolutionManagement {
            repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
            repositories { google(); mavenCentral() }
          }
          rootProject.name = "worldtv"
          include(":app")
          EOF

          # build.gradle (root) — vide (on utilise la DSL plugins au module)
          cat > build.gradle <<'EOF'
          // repositories in settings.gradle; plugins declared in app/build.gradle
          EOF

          # app/build.gradle — AGP 8.5.2 + Gradle 8.7 + Media3 1.4.1
          cat > app/build.gradle <<'EOF'
          plugins {
            id 'com.android.application' version '8.5.2'
          }

          android {
            namespace "com.worldtv"
            compileSdk 34
            defaultConfig {
              applicationId "com.worldtv"
              minSdk 21
              targetSdk 34
              versionCode 1
              versionName "1.0"
            }
            buildTypes { release { minifyEnabled false } }
            compileOptions {
              sourceCompatibility JavaVersion.VERSION_17
              targetCompatibility JavaVersion.VERSION_17
            }
          }

          dependencies {
            implementation 'androidx.appcompat:appcompat:1.6.1'
            implementation 'com.google.android.material:material:1.9.0'
            implementation 'androidx.media3:media3-exoplayer:1.4.1'
            implementation 'androidx.media3:media3-exoplayer-hls:1.4.1'
            implementation 'androidx.media3:media3-ui:1.4.1'
          }
          EOF

          # Manifest : MainActivity = launcher (exported), PlayerActivity sans intent-filter
          cat > app/src/main/AndroidManifest.xml <<'EOF'
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
            <application
                android:label="World TV"
                android:theme="@style/Theme.Material3.DayNight.NoActionBar">

              <activity
                  android:name=".MainActivity"
                  android:exported="true">
                <intent-filter>
                  <action android:name="android.intent.action.MAIN"/>
                  <category android:name="android.intent.category.LAUNCHER"/>
                </intent-filter>
              </activity>

              <activity android:name=".M3UInputActivity" android:exported="false"/>
              <activity android:name=".PlayerActivity" android:exported="false"/>

            </application>
          </manifest>
          EOF

          # Layouts
          cat > app/src/main/res/layout/activity_main.xml <<'EOF'
          <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:padding="24dp"
              android:gravity="center"
              android:layout_width="match_parent"
              android:layout_height="match_parent">
              <EditText
                  android:id="@+id/url_input"
                  android:hint="https://.../playlist.m3u8"
                  android:inputType="textUri"
                  android:layout_width="match_parent"
                  android:layout_height="wrap_content" />
              <Button
                  android:id="@+id/btn_play"
                  android:text="Lire M3U/M3U8"
                  android:layout_width="match_parent"
                  android:layout_height="wrap_content"
                  android:layout_marginTop="16dp" />
          </LinearLayout>
          EOF

          cat > app/src/main/res/layout/activity_player.xml <<'EOF'
          <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent">
              <androidx.media3.ui.PlayerView
                  android:id="@+id/player_view"
                  android:layout_width="match_parent"
                  android:layout_height="match_parent"/>
          </FrameLayout>
          EOF

          # strings.xml
          cat > app/src/main/res/values/strings.xml <<'EOF'
          <resources><string name="app_name">World TV</string></resources>
          EOF

          # Java
          cat > app/src/main/java/com/worldtv/MainActivity.java <<'EOF'
          package com.worldtv;

          import android.app.Activity;
          import android.content.Intent;
          import android.os.Bundle;
          import android.widget.Button;
          import android.widget.EditText;

          public class MainActivity extends Activity {
              @Override protected void onCreate(Bundle b) {
                  super.onCreate(b);
                  setContentView(R.layout.activity_main);
                  EditText url = findViewById(R.id.url_input);
                  Button play = findViewById(R.id.btn_play);
                  play.setOnClickListener(v -> {
                      String u = url.getText().toString().trim();
                      Intent i = new Intent(this, PlayerActivity.class);
                      i.putExtra("url", u);
                      startActivity(i);
                  });
              }
          }
          EOF

          cat > app/src/main/java/com/worldtv/PlayerActivity.java <<'EOF'
          package com.worldtv;

          import android.net.Uri;
          import android.os.Bundle;
          import androidx.appcompat.app.AppCompatActivity;
          import androidx.media3.common.MediaItem;
          import androidx.media3.exoplayer.ExoPlayer;
          import androidx.media3.ui.PlayerView;

          public class PlayerActivity extends AppCompatActivity {
              private ExoPlayer player;
              @Override protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  setContentView(R.layout.activity_player);
                  String url = getIntent().getStringExtra("url");
                  if (url == null || url.isEmpty()) {
                      url = "https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8";
                  }
                  player = new ExoPlayer.Builder(this).build();
                  PlayerView pv = findViewById(R.id.player_view);
                  pv.setPlayer(player);
                  player.setMediaItem(MediaItem.fromUri(Uri.parse(url)));
                  player.prepare(); player.play();
              }
              @Override protected void onDestroy() {
                  super.onDestroy();
                  if (player != null) { player.release(); player = null; }
              }
          }
          EOF

      - name: Build Debug APK (Gradle 8.7)
        run: gradle --no-build-cache assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: WorldTV-debug-apk
          path: app/build/outputs/apk/debug/*.apk
